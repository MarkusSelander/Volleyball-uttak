// This component is no longer needed as the team selection logic
// has been moved directly into the DashboardClient component
// using the same design pattern as the potential players section.

export default function TeamSelectionSection() {
  return null;
}
